11/02/2026
Array: array mehtods(push,pop,unshift,shift)


12/02/2025
string function

      -String length
       String charAt()
       String concat()
       String substring()
       String toUpperCase()
       String toLowerCase()
       String trim()
       String split()
       STRTING JOIN

for loop(used for iteration with each element )
       for of(use in array)
       for in(use in array and object)
       for each()
       map
       filter
       flat
